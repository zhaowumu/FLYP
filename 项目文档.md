# FLYP - 游戏开发项目管理系统 项目文档

## 一、项目概述

FLYP 是一款面向游戏开发团队的项目管理与缺陷跟踪系统，采用前后端分离架构。系统提供项目管理、任务分解、BUG 跟踪、角色权限控制、数据导入导出及钉钉通知等核心功能，帮助团队高效协作。

---

## 二、技术栈

### 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js + Express | 5.x | Web 框架 |
| TypeScript | 6.x | 开发语言 |
| TypeORM | 0.3.x | ORM 框架 |
| better-sqlite3 | 12.x | 嵌入式数据库 |
| PostgreSQL (pg) | 8.x | 关系型数据库（可选） |
| JWT (jsonwebtoken) | 9.x | 身份认证 |
| bcryptjs | 3.x | 密码加密 |
| multer | 2.x | 文件上传 |
| xlsx | 0.18.x | Excel 导入导出 |
| helmet / cors / morgan | - | 安全与日志中间件 |
| dingtalk-robot | 1.x | 钉钉机器人集成 |

### 前端

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 (组合式 API) | 3.4.x | 前端框架 |
| TypeScript | 5.3.x | 开发语言 |
| Vite | 5.x | 构建工具 |
| Element Plus | 2.8.x | UI 组件库 |
| Pinia | 2.1.x | 状态管理 |
| Vue Router | 4.2.x | 路由管理 |
| Axios | 1.6.x | HTTP 客户端 |
| wangEditor | 5.x | 富文本编辑器 |

---

## 三、项目结构

```
FLYP/
├── backend/                              # 后端服务
│   ├── src/
│   │   ├── config/                       # 配置模块
│   │   │   ├── database.ts               # TypeORM 数据源配置
│   │   │   ├── index.ts                  # 环境变量加载（dotenv）
│   │   │   └── permissions.ts            # 角色权限默认定义
│   │   ├── entities/                     # 数据库实体（6 个）
│   │   │   ├── User.ts                   # 用户实体
│   │   │   ├── Project.ts                # 项目实体
│   │   │   ├── Task.ts                   # 任务实体（支持子任务与依赖）
│   │   │   ├── Bug.ts                    # 缺陷实体
│   │   │   ├── OperationLog.ts           # 操作日志实体（多态关联）
│   │   │   └── SystemConfig.ts           # 系统配置（键值对存储）
│   │   ├── controllers/                  # 业务控制器（8 个）
│   │   │   ├── userController.ts         # 用户注册/登录/CRUD
│   │   │   ├── projectController.ts      # 项目 CRUD 与成员管理
│   │   │   ├── taskController.ts         # 任务 CRUD/状态/子任务/依赖/评论
│   │   │   ├── bugController.ts          # BUG CRUD/状态/分配/评论/统计
│   │   │   ├── uploadController.ts       # 图片/视频文件上传
│   │   │   ├── excelController.ts        # Excel 导入导出
│   │   │   ├── backupController.ts       # 数据备份/恢复/清空
│   │   │   └── permissionController.ts   # 权限查询与更新
│   │   ├── middleware/
│   │   │   └── authMiddleware.ts         # JWT 认证 + 角色中间件
│   │   ├── routes/                       # 路由定义（8 个）
│   │   │   ├── userRoutes.ts             # /api/users
│   │   │   ├── projectRoutes.ts          # /api/projects
│   │   │   ├── taskRoutes.ts             # /api/tasks
│   │   │   ├── bugRoutes.ts              # /api/bugs
│   │   │   ├── uploadRoutes.ts           # /api/upload
│   │   │   ├── excelRoutes.ts            # /api/excel
│   │   │   ├── backupRoutes.ts           # /api/backup
│   │   │   └── permissionRoutes.ts       # /api/permissions
│   │   ├── services/                     # 业务服务层
│   │   │   ├── dingtalkService.ts        # 钉钉 Webhook 通知服务
│   │   │   └── excelService.ts           # Excel 生成与解析服务
│   │   ├── app.ts                        # Express 应用配置
│   │   └── server.ts                     # 服务入口（DB 初始化 + 种子数据）
│   ├── uploads/                          # 上传文件存储
│   │   ├── images/
│   │   └── videos/
│   ├── data/                             # SQLite 数据库文件
│   ├── .env                              # 环境变量配置
│   ├── package.json
│   └── tsconfig.json
├── frontend/                             # 前端应用
│   ├── src/
│   │   ├── api/                          # API 接口封装
│   │   │   ├── index.ts                  # Axios 实例 + 拦截器
│   │   │   ├── user.ts                   # 用户相关接口
│   │   │   ├── project.ts                # 项目相关接口
│   │   │   ├── task.ts                   # 任务相关接口
│   │   │   ├── bug.ts                    # BUG 相关接口
│   │   │   ├── permission.ts             # 权限接口
│   │   │   └── excel.ts                  # 导入导出 + 备份接口
│   │   ├── components/                   # 公共组件
│   │   │   └── RichEditor.vue            # wangEditor 富文本编辑器封装
│   │   ├── views/                        # 页面视图（10 个）
│   │   │   ├── Login.vue                 # 登录页
│   │   │   ├── Dashboard.vue             # 工作台（角色视图 + 快速任务 + 统计卡片）
│   │   │   ├── Projects.vue              # 项目列表（卡片式布局）
│   │   │   ├── ProjectDetail.vue         # 项目详情
│   │   │   ├── Tasks.vue                 # 任务列表（树形表格 + 多标签筛选）
│   │   │   ├── TaskDetail.vue            # 任务详情（操作日志 + 抽屉操作面板）
│   │   │   ├── Bugs.vue                  # BUG 列表（多标签筛选）
│   │   │   ├── BugDetail.vue             # BUG 详情（操作日志 + 抽屉操作面板）
│   │   │   ├── Users.vue                 # 用户管理（仅管理员）
│   │   │   └── Settings.vue              # 系统设置（权限矩阵 + 数据管理）
│   │   ├── router/
│   │   │   └── index.ts                  # 路由配置 + 导航守卫
│   │   ├── stores/
│   │   │   └── user.ts                   # Pinia 用户状态 + 权限计算
│   │   ├── App.vue                       # 根组件（侧边栏 + 顶栏布局）
│   │   ├── main.ts                       # 应用入口
│   │   └── style.css                     # 全局样式
│   ├── package.json
│   ├── vite.config.ts
│   └── tsconfig.json
├── install.bat                           # 一键安装依赖
├── start.bat                             # 一键启动服务
├── stop.bat                              # 一键停止服务
├── restart.bat                           # 一键重启服务
├── menu.bat                              # 交互式菜单
├── 策划文档.md                           # 项目策划文档
└── 项目文档.md                           # 本文档
```

---

## 四、数据库设计

### 4.1 实体关系

```
User (1) ─── (N) Task (assignee / creator)
User (1) ─── (N) Bug  (assignee / reporter)
User (1) ─── (N) Project (manager)
Project (1) ── (N) Task
Project (1) ── (N) Bug
Task (1) ─── (N) Task (parentTask / subtasks，自引用)
Task (M) ─── (N) Task (dependencies，多对多，通过 task_dependencies 关联表)
User (1) ─── (N) OperationLog (操作日志)
OperationLog (N) ── (1) Task (多态关联，targetType="task")
OperationLog (N) ── (1) Bug  (多态关联，targetType="bug")
```

### 4.2 核心实体字段

#### User（用户）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int (PK) | 主键 |
| username | string (unique) | 登录账号 |
| password | string (hashed) | 加密密码 |
| realName | string | 真实姓名 |
| phone | string | 手机号 |
| role | string | 角色（admin/project_manager/developer/designer/artist/tester），默认 "developer" |
| isActive | boolean | 是否启用，默认 true |
| createdAt | datetime | 创建时间 |
| updatedAt | datetime | 更新时间 |

#### Project（项目）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int (PK) | 主键 |
| name | string | 项目名称 |
| description | string | 项目描述（富文本） |
| status | string | 状态（active/archived/completed），默认 "active" |
| createdBy | int | 创建人 ID |
| manager | User (FK) | 项目负责人（多对一） |
| createdAt | datetime | 创建时间 |
| updatedAt | datetime | 更新时间 |

#### Task（任务）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int (PK) | 主键 |
| title | string | 任务标题 |
| description | text | 任务描述（富文本） |
| priority | string | 优先级（low/medium/high/urgent），默认 "medium" |
| status | string | 状态（pending/in_progress/completed/closed），默认 "pending" |
| dueDate | date | 截止日期 |
| project | Project (FK) | 所属项目（多对一） |
| assignee | User (FK) | 负责人（多对一） |
| creator | User (FK) | 创建人（多对一） |
| parentTask | Task (FK) | 父任务（自引用，多对一） |
| subtasks | Task[] | 子任务列表（一对多） |
| dependencies | Task[] | 依赖任务（多对多，通过 task_dependencies 关联表） |
| createdAt | datetime | 创建时间 |
| updatedAt | datetime | 更新时间 |

#### Bug（缺陷）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int (PK) | 主键 |
| title | string | BUG 标题 |
| description | text | BUG 描述（富文本） |
| severity | string | 严重程度（low/medium/high/critical），默认 "medium" |
| status | string | 状态（pending/assigned/fixing/fixed/verified/closed），默认 "pending" |
| reproduceSteps | text | 复现步骤（富文本） |
| project | Project (FK) | 所属项目（多对一） |
| assignee | User (FK) | 负责人（多对一） |
| reporter | User (FK) | 报告人（多对一） |
| dueDate | date | 截止日期 |
| createdAt | datetime | 创建时间 |
| updatedAt | datetime | 更新时间 |

#### SystemConfig（系统配置）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int (PK) | 主键 |
| key | string (unique) | 配置键 |
| value | text | 配置值 |
| description | string | 描述 |
| createdAt | datetime | 创建时间 |
| updatedAt | datetime | 更新时间 |

#### OperationLog（操作日志）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int (PK) | 主键 |
| targetType | string | 目标类型（"task" 或 "bug"） |
| targetId | int | 目标任务/BUG ID |
| user | User (FK) | 操作用户（多对一） |
| action | string | 操作类型（create/status_change/assign/comment/severity_change/priority_change 等） |
| oldStatus | string | 变更前状态 |
| newStatus | string | 变更后状态 |
| oldAssignee | string | 变更前负责人 |
| newAssignee | string | 变更后负责人 |
| oldPriority | string | 变更前优先级 |
| newPriority | string | 变更后优先级 |
| oldSeverity | string | 变更前严重程度 |
| newSeverity | string | 变更后严重程度 |
| oldDueDate | string | 变更前截止日期 |
| newDueDate | string | 变更后截止日期 |
| remark | text | 备注（富文本） |
| createdAt | datetime | 操作时间 |

---

## 五、API 接口

### 5.1 用户接口（/api/users）

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | /register | 用户注册 | 否 |
| POST | /login | 用户登录 | 否 |
| GET | / | 获取所有用户 | 是 |
| GET | /:id | 获取用户详情 | 是 |
| PUT | /:id | 更新用户信息 | 是 |
| DELETE | /:id | 删除用户 | 是 |

### 5.2 项目接口（/api/projects）

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | / | 获取所有项目 | 是 |
| POST | / | 创建项目 | 是 |
| GET | /:id | 获取项目详情 | 是 |
| PUT | /:id | 更新项目 | 是 |
| DELETE | /:id | 删除项目 | 是 |
| PATCH | /:id/archive | 归档项目 | 是 |
| POST | /:id/members | 添加项目成员 | 是 |
| DELETE | /:id/members/:userId | 移除项目成员 | 是 |

### 5.3 任务接口（/api/tasks）

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | / | 获取任务列表 | 是 |
| POST | / | 创建任务 | 是 |
| GET | /:id | 获取任务详情 | 是 |
| PUT | /:id | 更新任务 | 是 |
| DELETE | /:id | 删除任务 | 是 |
| PATCH | /:id/status | 更新任务状态 | 是 |
| POST | /:id/comments | 添加任务评论 | 是 |
| POST | /:id/subtasks | 添加子任务 | 是 |
| GET | /:id/dependencies | 获取任务依赖 | 是 |

### 5.4 BUG 接口（/api/bugs）

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | / | 获取 BUG 列表 | 是 |
| POST | / | 提交 BUG | 是 |
| GET | /:id | 获取 BUG 详情 | 是 |
| PUT | /:id | 更新 BUG | 是 |
| DELETE | /:id | 删除 BUG | 是 |
| PATCH | /:id/status | 更新 BUG 状态 | 是 |
| PATCH | /:id/assign | 分配 BUG | 是 |
| POST | /:id/comments | 添加 BUG 评论 | 是 |
| GET | /stats | 获取 BUG 统计 | 是 |

### 5.5 文件上传（/api/upload）

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | /image | 上传图片（10MB） | 是 |
| POST | /video | 上传视频（100MB） | 是 |

### 5.6 Excel 导入导出（/api/excel）

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /export/tasks | 导出任务 Excel | 是 |
| GET | /export/bugs | 导出 BUG Excel | 是 |
| GET | /export/all | 导出全部数据 | 是 |
| POST | /import/tasks | 导入任务数据 | 是 |
| POST | /import/bugs | 导入 BUG 数据 | 是 |
| GET | /template/tasks | 下载任务导入模板 | 是 |
| GET | /template/bugs | 下载 BUG 导入模板 | 是 |

### 5.7 数据备份（/api/backup）

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /export | 导出 JSON 备份 | 是 |
| POST | /import | 从 JSON 恢复备份 | 是 |
| DELETE | /clear | 清空数据库（保留用户） | 是 |

### 5.8 权限管理（/api/permissions）

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | / | 获取当前权限配置 | 是 |
| PUT | / | 更新权限配置 | 是 |

---

## 六、前端路由

| 路径 | 页面 | 说明 | 权限 |
|------|------|------|------|
| /login | Login | 登录页 | 公开 |
| /dashboard | Dashboard | 工作台 | 已登录 |
| /projects | Projects | 项目列表（卡片式） | 已登录 |
| /projects/:id | ProjectDetail | 项目详情 | 已登录 |
| /tasks | Tasks | 任务列表（树形表格） | 已登录 |
| /tasks/:id | TaskDetail | 任务详情 | 已登录 |
| /bugs | Bugs | BUG 列表 | 已登录 |
| /bugs/:id | BugDetail | BUG 详情 | 已登录 |
| /users | Users | 用户管理 | 仅管理员 |
| /settings | Settings | 系统设置 | 仅管理员 |

---

## 七、权限体系

### 7.1 角色定义

| 角色 | 标识 | 说明 |
|------|------|------|
| 管理员 | admin | 拥有全部权限 |
| 项目经理 | project_manager | 项目管理 + 任务/BUG 管理 |
| 开发人员 | developer | 任务执行 + BUG 修复 |
| 美术人员 | artist | 任务执行 |
| 设计人员 | designer | 任务执行 |
| 测试人员 | tester | BUG 提交与验证 |

### 7.2 权限模型

系统采用 **双层权限** 设计：

1. **角色权限**：基于角色的基础操作权限，可在设置页动态配置
2. **关系权限**：基于用户与资源的关系（如负责人/创建人/报告人）授予额外权限

> 最终权限 = 角色权限 ∪ 关系权限

### 7.3 任务操作权限

| 操作 | 角色权限 | 关系权限 |
|------|----------|----------|
| 创建任务 | 配置决定 | - |
| 完成任务 | 配置决定 | 负责人可完成 |
| 重新打开 | 配置决定 | 创建人可重新打开 |
| 关闭任务 | 配置决定 | 创建人可关闭 |
| 转移任务 | 配置决定 | - |
| 修改优先级 | 配置决定 | - |
| 修改状态 | 配置决定 | - |
| 添加评论 | 配置决定 | 所有人可评论 |
| 删除任务 | 配置决定 | - |

### 7.4 BUG 操作权限

| 操作 | 角色权限 | 关系权限 |
|------|----------|----------|
| 创建 BUG | 配置决定 | - |
| 修复 BUG | 配置决定 | 负责人可修复 |
| 重新打开 | 配置决定 | 报告人可重新打开 |
| 验证 BUG | 配置决定 | 报告人可验证 |
| 关闭 BUG | 配置决定 | 报告人可关闭 |
| 分配 BUG | 配置决定 | - |
| 修改严重程度 | 配置决定 | - |
| 转移 BUG | 配置决定 | - |
| 修改状态 | 配置决定 | - |
| 添加评论 | 配置决定 | 所有人可评论 |
| 删除 BUG | 配置决定 | - |

---

## 八、状态流转

### 8.1 任务状态流转

```
待处理 (pending) ──→ 进行中 (in_progress) ──→ 已完成 (completed) ──→ 已关闭 (closed)
       ↑                      │                      │
       │                      ↓                      │
       └──────────────── 重新打开 ←───────────────────┘
```

### 8.2 BUG 状态流转

```
待处理 (pending) ──→ 已分配 (assigned) ──→ 修复中 (fixing) ──→ 已修复 (fixed) ──→ 已验证 (verified) ──→ 已关闭 (closed)
       ↑                    │                   │                   │                    │
       │                    ↓                   ↓                   ↓                    ↓
       └──────────── 重新打开 ←──────────────────────────────────────────────────────────┘
```

---

## 九、核心功能说明

### 9.1 工作台（Dashboard）

工作台根据用户角色展示不同的视图，每个角色拥有专属的统计数据和任务列表。

**通用元素**
- 欢迎横幅：渐变背景 + 浮动 SVG 图标动画 + Hero 图片展示
- 快速任务面板：3 个快捷入口卡片（每日任务 / 缺陷追踪 / 项目总览）
- 统计卡片：带渐变色条和图标，悬浮动效

**角色视图**

| 角色 | 视图内容 |
|------|----------|
| 管理员 | 全局统计（6 项）、项目进度、团队成员、待处理缺陷 TOP5、即将到期 TOP5 |
| 项目经理 | 项目统计（4 项）、管理的项目列表、成员负载面板 |
| 开发人员 | 任务统计（4 项）、待办任务、待修复缺陷、本周进度环形图、即将到期 |
| 美术人员 | 任务统计（3 项）、美术任务列表、完成统计环形图 |
| 设计人员 | 统计（3 项）、创建的策划任务、报告的缺陷 |
| 测试人员 | 统计（3 项）、报告的缺陷、待验证缺陷 |

### 9.2 任务管理

- **多标签筛选**：我的任务 / 我创建的 / 指派给我的 / 全部
- **树形表格**：支持 3 层子任务嵌套展示
- **列筛选**：按状态、优先级、负责人筛选
- **操作面板**：任务详情页右侧抽屉式操作面板，支持完成、重新打开、关闭、转移、修改优先级、修改状态、添加备注等操作
- **操作日志**：完整记录每次状态变更、负责人变更、评论等操作
- **任务依赖**：支持设置任务间的前置依赖关系

### 9.3 BUG 管理

- **多标签筛选**：我的 BUG / 我报告的 / 指派给我的 / 全部
- **列筛选**：按状态（6 种）、严重程度（4 级）、负责人筛选
- **操作面板**：与任务详情一致的抽屉式操作面板
- **复现步骤**：支持富文本编辑 BUG 复现步骤
- **BUG 统计**：按状态维度统计 BUG 数量

### 9.4 富文本编辑器

- 基于 wangEditor 5 封装
- 支持：撤销/重做、文本格式化、标题、字号、字体、颜色、列表、待办、对齐、引用、代码、链接、图片、视频、表格、表情、全屏
- 图片上传：最大 10MB，支持 JPEG/PNG/GIF/WebP/BMP
- 视频上传：最大 100MB，支持 MP4/WebM/OGG/AVI
- 插入视频对话框：支持 URL、宽度、高度、宽高比（16:9/4:3/1:1/自定义）
- 支持 Ctrl+V 直接粘贴图片

### 9.5 数据导入导出

- 导出任务/BUG/全部数据为 Excel 文件（中文标签）
- 从 Excel 导入任务/BUG 数据（按姓名匹配用户）
- 下载空白导入模板
- JSON 格式全量备份与恢复（备份文件仅存储关联实体的 ID 引用，不冗余存储完整用户数据）
- 清空数据库（保留用户数据）

#### JSON 备份格式

备份文件版本为 `1.0`，结构如下：

```json
{
  "version": "1.0",
  "timestamp": "2026-04-06T00:00:00.000Z",
  "data": {
    "users": [{ "id": 1, "username": "admin", "password": "...", "realName": "...", "phone": "...", "role": "admin", "isActive": true, "createdAt": "...", "updatedAt": "..." }],
    "projects": [{ "id": 1, "name": "...", "description": "...", "status": "...", "createdBy": 1, "managerId": 1, "createdAt": "...", "updatedAt": "..." }],
    "tasks": [{ "id": 1, "title": "...", "description": "...", "priority": "...", "status": "...", "dueDate": "...", "projectId": 1, "assigneeId": 1, "creatorId": 1, "parentTaskId": null, "createdAt": "...", "updatedAt": "..." }],
    "bugs": [{ "id": 1, "title": "...", "description": "...", "severity": "...", "status": "...", "reproduceSteps": "...", "dueDate": "...", "projectId": 1, "assigneeId": 1, "reporterId": 1, "createdAt": "...", "updatedAt": "..." }],
    "logs": [{ "id": 1, "targetType": "task", "targetId": 1, "userId": 1, "action": "...", "oldStatus": "...", "newStatus": "...", "createdAt": "..." }],
    "configs": [{ "id": 1, "key": "...", "value": "...", "description": "...", "createdAt": "...", "updatedAt": "..." }]
  }
}
```

所有关联字段均以 `Id` 后缀存储（如 `managerId`、`assigneeId`、`projectId`、`userId` 等），不嵌套完整对象，确保备份文件简洁且易于阅读。

### 9.6 系统设置

- **权限矩阵**：6 角色 × 任务操作（8 项）+ 6 角色 × BUG 操作（9 项），可动态切换配置
- **数据管理**：Excel 导入导出 + JSON 备份恢复

---

## 十、安装与运行

### 环境要求

- Node.js 18+
- npm 或 yarn

### 方式一：一键脚本

```bat
install.bat    :: 安装依赖
start.bat      :: 启动服务
stop.bat       :: 停止服务
restart.bat    :: 重启服务
menu.bat       :: 交互式菜单
```

### 方式二：手动运行

```bash
# 后端
cd backend
npm install
npm run dev

# 前端
cd frontend
npm install
npm run dev
```

### 访问地址

| 服务 | 地址 |
|------|------|
| 前端 | http://localhost:5173 |
| 后端 | http://localhost:3000 |

### 默认账号

| 账号 | 密码 | 角色 |
|------|------|------|
| admin | 123456 | 管理员 |
| pm001 | 123456 | 项目经理 |
| dev001 | 123456 | 开发人员 |
| test001 | 123456 | 测试人员 |
| artist001 | 123456 | 美术人员 |

### 环境变量配置（backend/.env）

```env
# 服务器配置
PORT=3000

# JWT 配置
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=7d

# 钉钉 Webhook
DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=your_token

# CORS 配置
CORS_ORIGIN=http://localhost:5173
```

---

## 十一、构建与部署

### 生产环境构建

```bash
# 后端
cd backend
npm run build    # 编译为 dist/
npm start        # 运行 dist/server.js

# 前端
cd frontend
npm run build    # 编译为 dist/
```

### 生产环境注意事项

1. 设置安全的 `JWT_SECRET`
2. 将 TypeORM 的 `synchronize` 设为 `false`
3. 配置正确的 `CORS_ORIGIN`
4. 配置钉钉 Webhook 地址
5. 前端 dist 目录需通过 Nginx 等静态服务器托管

---

## 十二、设计模式与架构特点

| 特点 | 说明 |
|------|------|
| 分层架构 | 配置 → 实体 → 中间件 → 路由 → 控制器 → 服务，职责清晰 |
| 操作日志 | 独立的 `OperationLog` 表，通过 `targetType` + `targetId` 多态关联 Task 和 Bug |
| 双层权限 | 角色权限 + 关系权限，支持运行时动态配置 |
| 层级任务 | 支持父子任务（自引用）+ 任务依赖（多对多） |
| 种子数据 | 启动时自动创建管理员和测试用户 |
| 富文本贯穿 | 项目描述、任务描述、BUG 描述、复现步骤、备注均使用富文本编辑器 |
| 抽屉式操作 | 详情页面通过右侧抽屉执行复杂操作（状态变更、转移、评论等） |
| 角色视图 | 工作台根据 6 种角色展示不同的统计数据和任务列表 |
