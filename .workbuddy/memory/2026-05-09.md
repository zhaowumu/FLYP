# 2026-05-09

## FLYP Dashboard 操作历史修复

**问题**：Dashboard 操作历史只显示 "任务 #123"，没有显示任务/缺陷的标题。

**原因**：后端 API `/api/operation-logs` 已经返回了 `title` 字段（任务/缺陷标题），但前端 Dashboard.vue 模板只显示了 ID，没有使用 title 字段。

**修复**：
- 修改 `Dashboard.vue` 第 248-255 行，在操作描述后面添加任务标题显示
- 新格式：`张三 创建了任务 《实现登录功能》` 或 `张三 将状态从「待处理」变更为「进行中」 《实现登录功能》`
- 添加 `.timeline-target` 样式，标题以蓝色显示，带省略号处理
- 移除了原来的 "任务/缺陷 #ID" 标签，简化显示

**文件**：`frontend/src/views/Dashboard.vue`
