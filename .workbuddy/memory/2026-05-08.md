# 2026-05-08

- 修改 FLYP 创建项目/任务/Bug 弹窗：宽度改为 90%，高度 max-height: 94vh + top: 3vh，接近全屏
- 调整表单顺序：分类标签从描述之后移到标题之后（任务名 → 分类 → 描述）
- 涉及文件：Tasks.vue、Bugs.vue、Projects.vue
- UI 偏好记录：用户偏好对话框/模态框几乎全屏显示
- 实现定时自动备份功能：node-cron 每天凌晨3点自动备份 SQLite db 文件到 data/backups/，保留最近30份
- 新建 backend/src/services/backupService.ts（cron调度、WAL checkpoint、文件拷贝、旧备份清理）
- 改造 backend/src/controllers/backupController.ts（增加状态查询、备份列表、下载/删除指定备份、立即备份接口）
- 改造 backend/src/routes/backupRoutes.ts（新增 /status、/list、/download/:filename、/file/:filename、/backup-now 路由）
- 改造 frontend/src/api/excel.ts（增加对应 API 封装）
- 改造 frontend/src/views/Settings.vue（备份Tab增加自动备份状态卡片、备份文件列表表格、立即备份按钮）
- 重新设计 Dashboard.vue 角色主页：从 7 种角色视图精简为 3 种（管理员/项目经理/开发人员统一）
  - admin: 系统级数据，新增项目健康指示器（红/黄/绿圆点），全局本周完成统计
  - project_manager: 项目进度 + 成员负载 + 新增成员任务完成情况卡片网格（待处理/进行中/已完成/完成率/待修缺陷）
  - developer/artist/designer/tester: 统一视图（待办任务+待处理缺陷+本周进度环形图+即将到期），tester/designer 底部额外显示角色专属区块
  - Banner 改为 3 种渐变 + 角色副标题，Quick Quest 按角色定制快捷入口
  - 新增计算属性: bannerClass, bannerSubtitle, quickQuests, adminCompletedThisWeek, memberCompletionData, roleExtraSections, pmPendingCount, pmOpenBugCount
  - 构建通过 ✓
- **全局样式统一（Design Tokens）**：
  - 重写 style.css 为完整设计令牌系统（颜色、字体、间距、圆角、阴影等 CSS 变量）
  - 优化 App.vue 侧边栏和整体布局引用设计令牌
  - 统一 Dashboard.vue 样式，替换所有硬编码颜色
  - 统一 Tasks.vue 样式（page-header、content-card、table-toolbar、task-cell、el-table 等）
  - 统一 Bugs.vue 样式（page-header、content-card、filter-tab、bug-title 等）
  - 统一 Projects.vue 样式（page-header、project-grid、project-card 等）
  - 统一 Users.vue 样式（page-header、content-card、user-cell、user-avatar 等）
  - 统一 Settings.vue 样式（permissions-container、export-section、backup-section、cloud-backup 等）
  - 统一 Login.vue 样式（渐变背景改用 --nb-gradient-login、卡片阴影等）
  - 统一 BugDetail.vue 样式（content-card、edit-btn、section-content、activity-remark、drawer 等）
  - 统一 TaskDetail.vue 样式（content-card、description-content、subtask、activity、sidebar、drawer 等）
  - 统一 ProjectDetail.vue 样式（page-header、content-card、stat-item、member-item 等）
  - 统一 MarkdownViewer.vue 样式（page-header、content-card）
  - 统一 Dashboard.vue SVG 环形图颜色（donut-bg、donut-ring）
- 实现 Gitee 云端备份功能（方案A：集成在项目内部）
  - 新建 backend/src/services/giteeBackupService.ts（Gitee API v5 上传 .db 备份文件，支持配置读写、测试连接、异步上传）
  - 修改 backupService.ts：备份完成后异步调用 uploadBackupToGitee 上传（不阻塞备份流程）
  - 扩展 systemConfigController.ts：增加 getGiteeBackupConfig / updateGiteeBackupConfig / testGiteeBackupConnection
  - 扩展 systemConfigRoutes.ts：增加 GET/PUT /gitee-backup + POST /gitee-backup/test
  - 扩展 frontend/src/api/systemConfig.ts：增加 getGiteeBackupConfig / updateGiteeBackupConfig / testGiteeBackupConnection
  - 扩展 Settings.vue 数据备份 Tab：新增 Gitee 云端备份配置区（开关、Token、仓库路径、分支、保存/测试连接）
  - 配置存储在 SystemConfig 表，key 前缀 gitee_backup_
  - 云端文件按月归档：backups/YYYY-MM/filename.db
  - 前后端编译通过 ✓
- 全局图标优化：
  - App.vue 侧边栏 Logo + index.html favicon 改为 bee.png（统一品牌形象）
  - Login.vue 登录页 Logo 从 Element Plus Monitor 图标改为 bee.png
  - Dashboard.vue hero 图片从 assets/hero.png 改为 /bee.png
  - Dashboard.vue 中所有 📁 emoji 替换为 `<el-icon><Folder /></el-icon>`，👤 emoji 替换为纯文本
  - 清理脚手架残留：删除 hero.png、vite.svg、vue.svg、HelloWorld.vue
