# 2026-05-12

## 为任务新增 testing（测试中）状态

完成全链路改造，新增任务状态 `testing`，流转：`completed → testing → closed`。

### 新增状态流转
- **pending**: 未分配负责人，可【指派】
- **in_progress**: 开发中，可【转交】【完成】
- **completed**: 开发完成，可【打回】【提测】【关闭】
- **testing**: 测试中，可【通过】【打回】
- **closed**: 已关闭，可【重启】

### 修改文件（9个）
1. **backend/src/controllers/taskController.ts** - `updateTaskStatus` 增加 `completed→testing` 分支；新增 `passTestTask`、`rejectTestTask` 方法
2. **backend/src/routes/taskRoutes.ts** - 新增 `/pass-test`、`/reject-test` 路由
3. **backend/src/services/excelService.ts** - 导出导入映射加 `testing`
4. **frontend/src/api/task.ts** - 新增 `submitForTest`、`passTestTask`、`rejectTestTask` API
5. **frontend/src/views/TaskDetail.vue** - 新增【提测】【通过】【打回】按钮、操作面板、状态映射
6. **frontend/src/views/Tasks.vue** - 筛选选项、状态颜色
7. **frontend/src/components/GlobalSearch.vue** - 搜索标签映射
8. **frontend/src/views/Dashboard.vue** - 状态文本、标签样式、成员统计加 `testingCount`
9. **frontend/src/stores/user.ts** - 新增 `submitTest`/`passTest`/`rejectTest` 权限动作

### 颜色规范
- testing 状态使用紫色 `#722ed1`；在 Dashboard 标签中使用 `tag-pink`（粉色）

### 终审后补修
- Dashboard.vue 删除 `})` 导致构建报错 Unexpected token → 已补回（原因为编辑时误删 teamMembers computed 的闭合行）
- ProjectDetail.vue taskStats 中 testing 未被计入"进行中"统计 → 已修复
- submitForTest API 数据结构错误（remark/action 未嵌套 log 中）→ 已修复
- **钉钉通知修复**：
  - bugController 创建时 `bug.assignee` 只有 `{id}` 没有 `phone`/`realName` → 改用 `userRepository.findOne` 单独加载
  - 分离任务和Bug的创建通知模板：`create` → `create_task` / `create_bug`，各自独立模板配置
  - 影响文件：bugController.ts（fix+type改名）、taskController.ts（type改名）、dingtalkService.ts（新增模板）、systemConfigController.ts（notifyTypes+测试模板）、Settings.vue（前端配置页面）、api/systemConfig.ts（注释更新）
- **钉钉通知重构**：删除通用 status_change/assignee_change，替换为 11 个具体操作类型
  - 任务：assign_task / complete_task / reject_task / submit_test_task / pass_test_task（+保留 create_task）
  - Bug：assign_bug / fix_bug / verify_bug / reject_bug（+保留 create_bug）
  - 删除 addComment 和 restart 场景的通知
  - 影响文件同上 6 个
