# 2026-05-11

## 钉钉通知系统重构
- **dingtalkService.ts**: 重构核心服务，`sendNotification` 新增 `atMobiles` 参数支持 @ 提及；新增 `{detailLink}` 变量自动生成标题链接；移除 `priority_change` 通知类型；重新设计默认模板（标题链接+@提及）
- **taskController.ts**: 创建任务时传递 `assigneeName`/`assigneePhones` 及 phones `atMobiles`；移除所有 `priority_change` 钉钉通知调用（updateTask 和 addComment 中）；`assignee_change` 通知附带新负责人手机号 @ 提及
- **bugController.ts**: 创建 BUG 时传递 `assigneeName`/`assigneePhones` 及 phones `atMobiles`；所有 `assignee_change` 通知附带新负责人手机号 @ 提及
- **systemConfigController.ts**: 移除 `priority_change` 通知类型；`testDingTalkNotification` 支持按 `type` 参数渲染对应类型测试模板
- **前端 systemConfig.ts**: 更新 API 签名支持 `baseUrl`/`notify`；新增 `testDingTalkByType(type?)` 导出
- **前端 Settings.vue**: 每个通知模板右侧添加独立「测试发送」按钮；移除优先级变更模板；更新模板变量说明（新增 `detailLink`/`assigneePhones`/@ 提及说明）；重新设计模板 UI 布局
