# 2026-05-11 工作日志

## 项目全面排查
- 对 FLYP 项目做了全面检查，发现核心问题：
  1. 后端权限中间件（taskPermissionMiddleware/bugPermissionMiddleware/projectPermissionMiddleware）已定义，部分已接入路由（bugRoutes、taskRoutes、projectRoutes），但配置项中存在死配置
  2. 前端权限大部分使用 `getTaskPermission/getBugPermission` 统一处理，少量综合操作硬编码 `role === 'admin'`
  3. PM Dashboard 快捷入口仍有「我的项目」「团队待办」「需关注」遗留

## 清理后端权限配置
- 清理 `backend/src/config/permissions.ts`：
  - `DEFAULT_ROLE_PERMISSIONS`：删除 task.complete/reject/close/transfer/changePriority/changeStatus；删除 bug.fix/rejectBug/close/transfer/verify/changeSeverity/changeStatus
  - `TASK_ACTION_LABELS`：仅保留 create/assign/restart/comment/delete/extendDueDate
  - `BUG_ACTION_LABELS`：仅保留 create/assign/restartBug/comment/delete/extendDueDate
  - 仅保留 Settings UI 中可配置的权限项，前端硬编码权限设置不变
  - 清理后与前端 `Settings.vue` 的 `DEFAULT_PERMISSIONS` 完全对齐

## 发现的问题
- `bugRoutes.ts` 中 `PATCH /:id/reject` 使用了 `bugPermissionMiddleware("rejectBug")`，但 `rejectBug` 在原 `permissions.ts` 中已为所有角色 `false`，该路由后端校验一直阻塞状态，是预存问题
